# task tracker
Used libraries: psycopg(Posgresql), pydantic, fastAPI, passlib
Needs task_tracker database in your postgresql folder

For usage:
Type "fastapi dev main.py" in terminal
Go to [server url]/docs 
